<?php
/**
 * Intentionally empty. This placeholder theme exists only so the rescue
 * endpoint can point WordPress at a functions.php that is guaranteed not to
 * fatal, in place of the site's real (possibly broken) active theme.
 */
defined('ABSPATH') || exit;
